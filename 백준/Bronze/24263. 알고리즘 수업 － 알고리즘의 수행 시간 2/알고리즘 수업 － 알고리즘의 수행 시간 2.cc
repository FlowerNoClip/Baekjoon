#include <iostream>
using namespace std;

int main() {
    long long N;
    cin >> N;

    cout << N << endl;  // 연산 횟수 (입력받은 N)
    cout << "1" << endl; // 시간 복잡도 O(N)에서 차수는 1
    return 0;
}
