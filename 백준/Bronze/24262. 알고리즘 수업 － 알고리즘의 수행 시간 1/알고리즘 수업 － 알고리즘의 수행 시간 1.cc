#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;


int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cout << "1" << endl;   // 연산 횟수 (항상 1번 수행)
    cout << "0" << endl;   // 시간 복잡도 O(1)에서 차수는 0
    return 0;

}


